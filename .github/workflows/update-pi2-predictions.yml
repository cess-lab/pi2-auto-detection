name: Update Pi2 Predictions
run-name: Pi2 predictions updated by ${{ github.actor }} 
on: workflow_dispatch
jobs:
  update-predictions:
     name: Update Pi2 Predictions
     runs-on: ubuntu-latest
     permissions:
       contents: write
     env:
       GITHUB_TOKEN: ${{ secrets.PERSONAL_ACCESS_TOKEN }}
     steps:
     - name: Checkout repo 
       uses: actions/checkout@v4
       with: 
         lfs: true
     # - name: Set up MATLAB
     #  uses: matlab-actions/setup-matlab@v1
     # - name: Run script
       # uses: matlab-actions/run-command@v1
       # with:
         # command: StartPrediction
     - name: Commit and push
       uses: github-actions-x/commit@v2.9
       with:
         github-token: $GITHUB_TOKEN
         push-branch: 'main'
         commit-message: 'Update predictions'
         force-add: 'true'
         # files: predicted-pi2\* wdc-ae\* PredictedPi2.mat README.md FullRecords.md
         name: Adib Yusof
         email: adib.yusof@upm.edu.my

